<!--pages/notice/notice.wxml-->
<view wx:for="{{taglist}}"  class="page__modules">
  <navigator url="/noticeshow/index?id={{item.Id}}">
    <view class="noticeTitle">{{item.title}}</view>
    <view class="noticeIntro">{{item.author}}</view>
  </navigator>
</view>